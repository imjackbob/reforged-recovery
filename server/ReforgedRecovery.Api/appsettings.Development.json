{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },

  "//": "Local dev overrides. Safe defaults: SQLite + console 'email'. Never commit real secrets here — use `dotnet user-secrets` or environment variables instead. See README.",
  "Database": {
    "Provider": "Sqlite"
  },
  "Email": {
    "Provider": "Console"
  }
}
